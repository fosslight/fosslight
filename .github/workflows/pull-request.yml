# OSS Name : peter-evans-docker-compose-actions-workflow
# SPDX-License-Identifier: MIT

name: Pull requests fosslight

on:
  pull_request:
    branches:
      - '*'
jobs:
  check-commit-message:
    uses: ./jobs/check-commit-message.yml

  check-the-site:
    uses: ./jobs/check-site.yml

  test:
    uses: ./jobs/test.yml
